name: sklearn_hw5
conda_env: conda.yml
entry_points:
  main:
    parameters:
      train_path: path
      alpha: {type: float, default: 1.0}
    command: "python train.py {train_path} --alpha={alpha}"
